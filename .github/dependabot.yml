# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  - package-ecosystem: "npm"        # or "pip", "maven", "gradle", "docker", etc.
    directory: "/"                  # location of manifest files (e.g., package.json, requirements.txt)
    schedule:
      interval: "weekly"            # options: daily, weekly, monthly
    open-pull-requests-limit: 5     # optional: max number of PRs at once
    reviewers:                      # optional: auto-assign reviewers
      - "Joseph163-mufasa"
    labels:                         # optional: add labels to PRs
      - "dependencies"
